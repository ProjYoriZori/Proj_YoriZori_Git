# ============================================
# 다른 PC(클라이언트)에서 원격 서버 연결용 .env
# ============================================
# 사용법:
#   1. 이 파일을 .env 로 복사
#      Copy-Item .env.remote.example .env
#   2. npm install && npm start
# ============================================

# 서버 PC(172.30.122.174)의 Spring Boot API에 연결
EXPO_PUBLIC_API_BASE_URL=http://172.30.122.174:8080
